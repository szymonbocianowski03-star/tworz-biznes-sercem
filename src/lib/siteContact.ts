/** Zgodne z linkami w aplikacji (np. AppShell, stopka landing). */
export const SUPPORT_EMAIL = "support@marketingnow.tech";

export const partnerProgramMailto = `mailto:${SUPPORT_EMAIL}?subject=${encodeURIComponent(
  "Program partnerski MarketingNow — więcej szczegółów",
)}`;
